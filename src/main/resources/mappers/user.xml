<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.ssafy.lasttable.user.repository.UserMapper">

	<!-- 사용자 추가 -->
	<insert id="insertUser"
		parameterType="com.ssafy.lasttable.user.entity.User">
		INSERT INTO users (user_id, pwd, name, phone)
		VALUES
		(#{userId}, #{pwd}, #{name}, #{phone});
	</insert>

	<!-- 사용자 조회 (user_id로) -->
	<select id="getReservationsByUserId"
		resultType="com.ssafy.lasttable.reservation.entity.Reservation">
		SELECT *
		FROM reservation
		WHERE user_id = #{userId}
		ORDER BY
		CASE
		WHEN canceled_at IS NULL THEN 0
		ELSE 1
		END,

		CASE
		WHEN reserved_at >=
		CURDATE() THEN 0
		ELSE 1
		END,

		reserved_at ASC
	</select>


	<!-- 모든 사용자 조회 -->
	<select id="getAllUsers"
		resultType="com.ssafy.lasttable.user.entity.User">
		SELECT user_id, pwd, name, phone
		FROM users;
	</select>

	<!-- 아이디 중복 체크 -->
	<select id="checkUserIdExist" resultType="boolean">
		SELECT COUNT(*) > 0
		FROM users
		WHERE user_id = #{userId};
	</select>

	<update id="updateName">
		UPDATE users
		SET name = #{newName}
		WHERE user_id =
		#{userId}
	</update>

</mapper>
